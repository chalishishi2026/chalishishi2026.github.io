# 2026-05-13

## 肆顾门数字文化网站更新

### 完成的工作

1. **简化创作中心页面（submit.html）**
   - 重新设计界面，采用卡片式快捷操作（上传文档、预览效果、提交审核、我的作品）
   - 添加拖拽/点击上传功能，支持 .doc 和 .docx 格式
   - 集成 mammoth.js 自动将 Word 文档转换为可浏览的 HTML 界面
   - 文档内容实时预览
   - 表单作品类型新增 PIA 剧选项

2. **原创剧本馆添加 PIA 戏剧本分类（works.html）**
   - 筛选栏新增 "PIA剧" 按钮
   - 更新 main.js 的 getTypeText() 函数支持 PIA 剧类型

### 技术要点
- 使用 mammoth.js CDN 解析 docx 文件
- 前端直接转换，无需后端支持
- .doc 格式暂不支持自动转换（需用户另存为 .docx）

## 肆顾门后台管理重构 (17:03)

将后台管理从 CloudBase 云函数改为 **Node.js + Express + JSON 文件存储**：

- 创建 `server/` 目录，包含 Express 后端
- JSON 文件存储数据（news.json, team.json, works.json, admins.json）
- RESTful API 接口（auth, news, team, works）
- 重写 `admin.html` 对接本地 API
- 删除 `cloudfunctions/` 目录
- 默认账号: admin / admin123

启动方式：
```bash
cd sigumen-website/server
npm install
npm start
```
访问 http://localhost:3000/admin.html

## 肆顾门作品审核流程完善 (19:22)

### 问题修复

1. **用户上传后"我的作品"看不到记录**
   - 后端：works.js 添加 `userId` 字段，作品按用户ID筛选
   - 前端：submit.html 添加"我的作品"加载函数，登录后自动获取用户作品列表
   - 前端：main.js 登录/注册时生成并保存 `userId` 到 localStorage

2. **审核拒绝功能完善**
   - 后端：reject API 支持接收拒绝原因，存入 `rejectionReason` 字段
   - 后端：审核通过/拒绝时自动创建通知（notifications.json）
   - 前端：管理后台添加"拒绝原因"模态框（带快捷原因按钮）
   - 前端：作品预览显示拒绝原因（如果有）
   - 前端：被拒绝的作品在"我的作品"中显示拒绝原因

3. **审核通过的作品同步到前端**
   - 后端：works.html 从 API 获取 `status=approved` 的作品
   - 后端：移除硬编码假数据
   - 后端：作品筛选支持中文类型名（剧本、广播剧等）

### 新增文件
- `server/data/notifications.json` - 用户通知存储

### 修改文件
- `server/routes/works.js` - 添加userId、拒绝原因、通知系统
- `submit.html` - "我的作品"功能、用户通知弹窗、编辑重提交
- `admin.html` - 拒绝原因模态框、预览显示拒绝原因
- `assets/js/main.js` - 作品列表API对接、登录userId保存
